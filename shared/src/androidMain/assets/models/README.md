# ASTRA Android model assets

Place the Sprint 3 validation model here:

```text
astra-slm.tflite
```

The file is intentionally not committed by default because real model binaries can be large and may have separate licensing terms.

See:

```text
docs/REAL_INFERENCE_SETUP.md
```

