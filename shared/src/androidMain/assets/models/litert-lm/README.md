# LiteRT-LM model bundle

Place a compatible generative model bundle in this directory when validating the future LiteRT-LM path.

Expected minimum files:

```text
model.tflite
tokenizer.model
```

Optional:

```text
config.json
```

The actual model files are not committed by default because generative model binaries are large and often carry separate license terms.

